<?xml version="1.0" encoding="utf-8"?>
<resources
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- Input/Output Textview style -->
    <style name="Widget.Calculator.TextView" parent="Widget.AppCompat.TextView">
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:paddingStart">24dp</item>
        <item name="android:paddingEnd">24dp</item>
    </style>

    <!-- Divider style -->
    <style name="Widget.Calculator.Divider" parent="">
        <item name="android:background">?attr/dividerHorizontal</item>
        <item name="android:layout_marginStart">24dp</item>
        <item name="android:layout_marginEnd">24dp</item>
        <item name="android:layout_marginTop">4dp</item>
        <item name="android:layout_marginBottom">4dp</item>
    </style>

    <!-- Calculator Button style -->
    <style name="Widget.Calculator.CalcButton" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">20dp</item>
        <item name="android:layout_marginEnd">20dp</item>
        <item name="android:background">@drawable/button_background_selector</item>
        <item name="android:layout_gravity">fill</item>
        <item name="android:scaleType">fitCenter</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:tint">@color/calc_image_button_normal</item>
        <item name="backgroundTint">@color/calc_button_pressed</item>
    </style>
</resources>